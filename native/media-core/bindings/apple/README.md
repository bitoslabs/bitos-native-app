# Apple binding

Expose the stable C ABI to a thin Objective-C++/Swift adapter. This layer owns Apple pixel/audio buffers, thread affinity, cancellation, and conversion to value-only MediaCore requests. No SwiftUI or signing behavior belongs here.
