# OpenGL ES fallback

The bounded compatibility backend belongs here. It must declare unsupported effects explicitly and produce the same geometry, timing, and color-space contract within documented tolerance.
