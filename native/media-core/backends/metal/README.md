# Metal backend

Apple GPU implementation belongs here. It implements MediaCore render interfaces only; Swift/SwiftUI types stay in the Apple binding layer. Shader sources are versioned inputs to render cache keys and golden tests.

