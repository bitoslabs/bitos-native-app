import SwiftUI

struct DiscoverView: View {
    var body: some View {
        FeaturePlaceholder(
            title: "Discover",
            subtitle: "Creators, sounds, templates and communities",
            symbol: "sparkle.magnifyingglass"
        )
    }
}
