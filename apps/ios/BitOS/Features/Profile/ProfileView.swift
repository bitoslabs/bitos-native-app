import SwiftUI

struct ProfileView: View {
    var body: some View {
        FeaturePlaceholder(
            title: "Profile",
            subtitle: "Identity, projects and Creator Studio",
            symbol: "person.crop.circle.fill"
        )
    }
}
