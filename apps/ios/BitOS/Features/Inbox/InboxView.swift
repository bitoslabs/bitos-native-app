import SwiftUI

struct InboxView: View {
    var body: some View {
        FeaturePlaceholder(
            title: "Inbox",
            subtitle: "Activity, zaps and secure messages",
            symbol: "bell.badge.fill"
        )
    }
}
