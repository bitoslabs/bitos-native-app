import SwiftUI

struct HomeView: View {
    var body: some View {
        FeaturePlaceholder(
            title: "Bitz Feed",
            subtitle: "For You, Following and direct-relay fallback",
            symbol: "play.rectangle.fill"
        )
    }
}
