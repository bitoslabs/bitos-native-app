<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.BitOS" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#09090B</item>
        <item name="android:navigationBarColor">#09090B</item>
    </style>

    <!-- Launch theme (legacy Flutter LaunchTheme parity): the branded
         window background (bg color + centered logo) shows while the
         process starts; MainActivity swaps to Theme.BitOS before
         Compose mounts. Night densities resolve via drawable-night/. -->
    <style name="Theme.BitOS.Launch">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
