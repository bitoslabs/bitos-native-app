package space.bitos.app.data.media

/**
 * Default Blossom server for V1. Configurable per-account later (BUD-03
 * server lists); the local dev stack overrides this for its own testing.
 */
object DefaultBlossomServer {
    const val url: String = "https://blossom.primal.net"
}
