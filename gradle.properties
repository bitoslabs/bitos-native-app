org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.console.interactive=false
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
kotlin.mpp.enableCInteropCommonization=true

# Android release identity. Keep the name aligned with the v<name> Git tag;
# increment the code for every installable release.
BITOS_APP_VERSION_NAME=0.1.0
BITOS_APP_VERSION_CODE=1

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
